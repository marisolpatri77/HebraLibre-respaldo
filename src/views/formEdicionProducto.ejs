<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Formulario Edicion Productos</title>

        <link rel="stylesheet"  href="/css/formAltaProducto.css">
        <link href="/css/header-footer.css" rel="stylesheet">

        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Montserrat&family=Poppins:ital,wght@0,100;0,400;1,500&family=Roboto:wght@100&display=swap" rel="stylesheet">
        <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" />
    </head>
    <body>

        <%- include('./partials/header.ejs')%>

        <div class="contenedor">

            <h4>Edición de Producto</h4>
    
            <form action="/productos/edit/<%=producto.id%>?_method=PUT" method="POST" enctype="multipart/form-data" class="formulario">
                <div class="campo">
                    <label for="name">Nombre del Producto:</label>
                    <input type="text" id="name" name="title" class="form-input" value="<%= producto.title %>">
                </div>
                <div id="titleError" class="text-danger hidden"></div>
            
                <div class="campo">
                    <label for="name">Color del Producto:</label>
                    <input type="text" id="name" name="colors" class="form-input" value="<%= producto.colors %>">
                </div>
                <div id="colorsError" class="text-danger hidden"></div>
            
                <div class="campo">
                    <label for="category" class="form-label">Categoría:</label>
                    <select name="category" id="category" class="options">
                        <% if (categorias.name == 'Productos Principales') { %>
                            <option value="Productos Principales" selected>Productos Principales</option>
                            <option value="En Oferta">En Oferta</option>
                        <% } else { %> 
                            <option value="Productos Principales">Productos Principales</option>
                            <option value="En Oferta" selected>En Oferta</option>
                        <% } %>
                    </select>
                </div>
                <div id="categoryError" class="text-danger hidden"></div>
            
                <div class="campo">
                    <label for="price">Precio:</label>
                    <input type="text" id="price" name="price" step="0.01" class="form-input" value="<%= producto.price %>">
                </div>
                <div id="priceError" class="text-danger hidden"></div>
            
                <div class="campo">
                    <label for="description">Descripción:</label>
                    <textarea id="description" name="descripcion" rows="4" class="form-input"><%= producto.description %></textarea>
                </div>
                <div id="descripcionError" class="text-danger hidden"></div>
            
                <div class="campo">
                    <label for="discount">Descuento:</label>
                    <input type="text" id="discount" name="discount" value="<%= producto.discount %>">
                </div>
                <div id="discountError" class="text-danger hidden"></div>
            
                <div class="campo">
                    <label for="image">Imagen:</label>
                    <input type="file" id="image" name="img" accept="image/*" class="input-img form-input" value="<%= producto.img %>">
                </div>
                <div id="imgError" class="text-danger hidden"></div>
            
                <div class="botones">
                    <a href="/productos/catalogo"><button type="button" class="btn-cancelar">Cancelar</button></a>
                    <button class="btn-guardar" type="submit">Guardar</button>
                </div>
            </form>
            
            
        </div>
        <script src="/js/validationEditProduct.js"></script> 
    </body>
</html>
