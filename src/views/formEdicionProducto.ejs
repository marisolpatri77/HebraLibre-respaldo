<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Formulario Edicion Productos</title>

        <link rel="stylesheet"  href="/css/formAltaProducto.css">
        <link href="/css/header-footer.css" rel="stylesheet">

        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Montserrat&family=Poppins:ital,wght@0,100;0,400;1,500&family=Roboto:wght@100&display=swap" rel="stylesheet">
        <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" />
    </head>
    <body>

        <%- include('./partials/header.ejs')%>

        <div class="contenedor">

            <h4>Edición de Producto</h4>
    
            <form action="" method="post" class="formulario">
                <div class="campo">
                    <label for="name">Nombre del Producto:</label>
                    <input type="text" id="name" name="name" class="form-input" value="<%= idBuscado.name %>">
                </div>
    
                <div class="campo">
                    <label for="category">Categoría:</label>
                    <input type="text" id="category" name="category" class="form-input" value="<%= idBuscado.category %>">
                </div>
    
                <div class="campo">
                    <label for="price">Precio:</label>
                    <input type="number" id="price" name="price" step="0.01" class="form-input" value="<%= idBuscado.price %>">
                </div>
    
                <div class="campo">
                    <label for="description">Descripción:</label>
                    <textarea id="description" name="description" rows="4" class="form-input" value="<%= idBuscado.description %>"></textarea>
                </div>
    
                <div class="campo">
                    <label for="image">Imagen:</label>
                    <input type="file" id="image" name="image" accept="image/*" class="input-img" class="form-input" value="<%= idBuscado.image %>">
                </div>
    
                <div class="botones">
                    <input class="btn-cancelar" type="submit" value="Cancelar">
                    <input class="btn-guardar" type="submit" value="Guardar">
                </div>
            </form>
        </div>
    </body>
</html>
